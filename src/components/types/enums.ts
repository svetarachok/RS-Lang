export enum Endpoint {
  words = '/words',
  users = '/users',
  signin = '/signin',
  tokens = '/tokens',
}
export enum ContentType {
  json = 'application/json',
}
export enum HTTPMethod {
  GET = 'GET',
  POST = 'POST',
}
